// ltl-imap-list/main.ltl
//
// IMAP4rev1 LIST / SELECT / FETCH client.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-imap-list v0.1.0")
    io::println("IMAP4rev1 LIST / SELECT / FETCH client.")
    0
}
